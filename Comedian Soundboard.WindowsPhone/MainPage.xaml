<Page
    x:Class="Comedian_Soundboard.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Comedian_Soundboard"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    d:DataContext="{Binding Source={d:DesignData Source=../Comedian Soundboard.Shared/DataModel/Soundboard.json, Type=data:SoundDataSource}}"
    xmlns:data="using:Comedian_Soundboard.Data"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ListView SelectionMode="None"
                  ItemsSource="{Binding Groups}"
                  IsItemClickEnabled="True"
                  ItemClick="Group_Click"
                  HorizontalAlignment="Center"
                  ContinuumNavigationTransitionInfo.ExitElementContainer="True">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,30,0,0">
                        <TextBlock Text="{Binding Title}" FontSize="20" Margin="20,0,0,0" TextAlignment="Center" VerticalAlignment="Top"></TextBlock>
                        <Border BorderBrush="White" BorderThickness="2" Height="300" Width="300"></Border>
                        <Image Source="{Binding ImagePath}" Width="300" Height="300"></Image>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
