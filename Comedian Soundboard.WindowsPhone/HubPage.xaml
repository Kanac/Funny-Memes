<Page
    x:Class="Comedian_Soundboard.HubPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Comedian_Soundboard"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    d:DataContext="{Binding Source={d:DesignData Source=../Comedian Soundboard.Shared/DataModel/Soundboard.json, Type=data:SoundDataSource}}"
    xmlns:data="using:Comedian_Soundboard.Data"
    mc:Ignorable="d">

    

    <Grid x:Name="LayoutRoot">
        <MediaElement Name="Audio" AutoPlay="False" Visibility="Collapsed"></MediaElement>
        
        <Grid DataContext="{Binding Groups[3]}">
            <GridView 
                SelectionMode="None"
                IsItemClickEnabled="True"
                ItemsSource="{Binding SoundItems}"
                ItemClick="Sound_Click"
                HorizontalAlignment="Center"
                ContinuumNavigationTransitionInfo.ExitElementContainer="True">
                <GridView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Border BorderBrush="Aqua" BorderThickness="1" Height="70" Width="100"></Border>
                            <Rectangle Height="50" Width="100"></Rectangle>
                            <TextBlock Text="{Binding Subtitle}" TextAlignment="Center" VerticalAlignment="Center"></TextBlock>
                        </Grid>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
        </Grid>
    </Grid>
</Page>